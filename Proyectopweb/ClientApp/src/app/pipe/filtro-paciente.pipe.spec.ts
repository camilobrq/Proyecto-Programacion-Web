import { FiltroPacientePipe } from './filtro-paciente.pipe';

describe('FiltroPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
