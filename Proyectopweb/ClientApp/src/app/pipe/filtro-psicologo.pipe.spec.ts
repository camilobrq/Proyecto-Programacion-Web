import { FiltroPsicologoPipe } from './filtro-psicologo.pipe';

describe('FiltroPsicologoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPsicologoPipe();
    expect(pipe).toBeTruthy();
  });
});
