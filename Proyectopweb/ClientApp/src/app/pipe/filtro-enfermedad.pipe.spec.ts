import { FiltroEnfermedadPipe } from './filtro-enfermedad.pipe';

describe('FiltroEnfermedadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEnfermedadPipe();
    expect(pipe).toBeTruthy();
  });
});
