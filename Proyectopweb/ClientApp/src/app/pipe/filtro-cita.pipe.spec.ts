import { FiltroCitaPipe } from './filtro-cita.pipe';

describe('FiltroCitaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCitaPipe();
    expect(pipe).toBeTruthy();
  });
});
